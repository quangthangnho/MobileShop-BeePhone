<%@ page pageEncoding="utf-8"%>

<button  onclick="cart.add(${element.id}, `${element.name}`,`${element.image}`, `${element.unitPrice}`, ${element.discount}) , notify()" class="aa-add-card-btn"><span class="fa fa-shopping-cart"></span>Thêm vào giỏ hàng</button>
